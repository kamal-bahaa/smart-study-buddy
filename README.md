# 🎓 Smart Study Buddy — Backend & AI Services

**An AI-powered study assistant that transforms lecture PDFs into flashcards, quizzes, and summaries.**

[![Node.js](https://img.shields.io/badge/Node.js-22.x-339933?style=flat-square&logo=node.js&logoColor=white)](https://nodejs.org)
[![Express](https://img.shields.io/badge/Express-5.x-000000?style=flat-square&logo=express&logoColor=white)](https://expressjs.com)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16-4169E1?style=flat-square&logo=postgresql&logoColor=white)](https://postgresql.org)
[![Prisma](https://img.shields.io/badge/Prisma-ORM-2D3748?style=flat-square&logo=prisma&logoColor=white)](https://prisma.io)
[![Python](https://img.shields.io/badge/Python-3.11-3776AB?style=flat-square&logo=python&logoColor=white)](https://python.org)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.115-009688?style=flat-square&logo=fastapi&logoColor=white)](https://fastapi.tiangolo.com)

---

## 📖 Table of Contents

- [Overview](#-overview)
- [Architecture](#-architecture)
- [Tech Stack](#-tech-stack)
- [Database Schema](#-database-schema)
- [Project Structure](#-project-structure)
- [AI Services](#-ai-services)
- [Backend API](#-backend-api)
- [Authentication](#-authentication)
- [Environment Variables](#-environment-variables)
- [Getting Started](#-getting-started)
- [API Reference](#-api-reference)
- [Pipelines](#-pipelines)
- [Error Handling](#-error-handling)
- [Team](#-team)

---

## 🧠 Overview

Smart Study Buddy is an AI-powered graduation project that helps students study more effectively. Given a lecture PDF, the system automatically generates:

- 🃏 **Flashcards** — Question/answer pairs with difficulty levels (Easy / Medium / Hard), generated by a **fine-tuned LLaMA-3-8B** model trained specifically on educational Q&A data
- 📝 **MCQ Quiz** — Multiple-choice questions with shuffled options and full document coverage, generated by a **base LLaMA-3-8B Instruct** model
- 📄 **Summary** — Structured academic summary broken into main topic, key concepts, important details, and conclusion via **Groq llama-3.3-70b** — exportable as a styled PDF
- 🌐 **Translation** — Real-time Arabic translation of any selected text using **Groq**
- 📚 **History** — Unified activity feed tracking all generated content
- 👤 **Profile Management** — Update name, change password, delete account

All flashcards and MCQs are delivered via **Server-Sent Events (SSE)** so the user sees content appearing progressively in real time.

---

## 🏗 Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        Flutter App                              │
│              (Mobile Client — SSE + REST)                       │
└────────────────────────┬────────────────────────────────────────┘
                         │ HTTP / SSE
┌────────────────────────▼────────────────────────────────────────┐
│                   Node.js / Express API                         │
│         Auth · PDF · Flashcards · Quiz · Summary                │
│              Translation · Profile · History                    │
└──────┬──────────────┬──────────────────┬───────────────────────-┘
       │              │                  │
       ▼              ▼                  ▼
┌──────────┐  ┌──────────────┐  ┌────────────────┐
│PostgreSQL│  │  Groq API    │  │  Python AI     │
│ (Prisma) │  │ Summary +    │  │  Microservices │
│          │  │ Translation  │  │  (Colab+ngrok) │
└──────────┘  └──────────────┘  └───────┬────────┘
                                         │
                          ┌──────────────┴──────────────┐
                          │                             │
                    ┌─────▼──────┐             ┌────────▼───────┐
                    │ Flashcard  │             │  MCQ Service   │
                    │  Service   │             │  (port 8001)   │
                    │ (port 8000)│             │                │
                    │Fine-tuned  │             │ Base LLaMA-3   │
                    │ LLaMA-3-8B │             │ 8B Instruct    │
                    └────────────┘             └────────────────┘
```

### Core Pipelines

**PDF Text Extraction Pipeline**
```
PDF Upload → SHA-256 Hash Check → PyMuPDF (local extraction) → Groq (text cleaning) → PostgreSQL
```

> If the same PDF was uploaded before (same content, any filename), the server returns the existing document immediately without re-processing.

**Flashcard Generation Pipeline**
```
PDF Text → Node.js chunks (2000 chars) → FastAPI sub-chunks (600 chars)
         → Fine-tuned LLaMA-3-8B → Flashcard JSON → SSE Stream → Flutter
```

**MCQ Generation Pipeline**
```
PDF Text → Node.js → FastAPI continuous chunks → Base LLaMA-3-8B Instruct
         → MCQ JSON → SSE Stream → Flutter
```

---

## 🛠 Tech Stack

| Layer | Technology | Purpose |
|---|---|---|
| Runtime | Node.js 22.x | Backend server |
| Framework | Express 5.x | REST API framework |
| ORM | Prisma 5.x | Database access & migrations |
| Database | PostgreSQL 16 | Persistent data storage |
| Auth | JWT (access + refresh) | Stateless authentication |
| AI — Summary & Translation | Groq `llama-3.3-70b-versatile` | LLM inference |
| AI — Flashcards | Fine-tuned LLaMA-3-8B (Unsloth) | Educational Q&A generation |
| AI — MCQ | Base LLaMA-3-8B Instruct (Unsloth) | Multiple-choice questions |
| AI — PDF Cleaning | Groq + PyMuPDF | Text extraction & cleanup |
| Streaming | Server-Sent Events (SSE) | Real-time content delivery |
| Python Service | FastAPI + Uvicorn | AI microservice runtime |
| Tunneling | ngrok | Expose Colab notebooks |

---

## 🗄 Database Schema

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Difficulty {
  easy
  medium
  hard
}

model User {
  id           Int           @id @default(autoincrement())
  email        String        @unique
  password     String
  name         String?
  refreshToken String?
  createdAt    DateTime      @default(now())
  updatedAt    DateTime      @updatedAt

  pdfs         PdfDocument[]

  @@index([email])
}

model PdfDocument {
  id            Int         @id @default(autoincrement())
  fileName      String
  extractedText String      @db.Text
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt

  userId        Int
  user          User        @relation(fields: [userId], references: [id], onDelete: Cascade)

  summaries     Summary[]
  flashcards    Flashcard[]
  quizzes       Quiz[]

  hash  String?

  @@index([userId])
  @@index([createdAt])
}

model Summary {
  id        Int         @id @default(autoincrement())
  content   String      @db.Text
  createdAt DateTime    @default(now())

  pdfId     Int
  pdf       PdfDocument @relation(fields: [pdfId], references: [id], onDelete: Cascade)

  @@index([pdfId])
}

model Flashcard {
  id         Int         @id @default(autoincrement())
  front      String      @db.Text
  back       String      @db.Text
  difficulty Difficulty  @default(easy)
  createdAt  DateTime    @default(now())

  pdfId      Int
  pdf        PdfDocument @relation(fields: [pdfId], references: [id], onDelete: Cascade)

  @@index([pdfId])
}

model Quiz {
  id        Int         @id @default(autoincrement())
  title     String      @default("Quiz")
  createdAt DateTime    @default(now())

  pdfId     Int
  pdf       PdfDocument @relation(fields: [pdfId], references: [id], onDelete: Cascade)

  questions Question[]

  @@index([pdfId])
  @@index([createdAt])
}

model Question {
  id            Int      @id @default(autoincrement())
  text          String   @db.Text
  options       String[]
  correctAnswer String

  quizId        Int
  quiz          Quiz     @relation(fields: [quizId], references: [id], onDelete: Cascade)

  @@index([quizId])
}
```

### Relationships

```
User
 ├── PdfDocument []
 │    ├── Flashcard []
 │    ├── Quiz []
 │    │    └── Question []
 │    └── Summary []
 └── refreshToken
```

All child records are **cascade-deleted** when a `PdfDocument` or `User` is removed.

---

## 📁 Project Structure

```
smart-study-buddy-backend/
│
├── src/
│   ├── app.js                          # Express app setup & route mounting
│   ├── server.js                       # Server entry point (port, graceful shutdown)
│   │
│   ├── config/
│   │   └── env.js                      # Environment variable validation
│   │
│   ├── prisma/
│   │   └── client.js                   # Prisma client singleton
│   │
│   ├── middleware/
│   │   ├── authenticate.js             # JWT verification middleware
│   │   ├── errorHandler.js             # Global error handler (ApiError, Prisma P2002)
│   │   └── notFound.js                 # 404 handler
│   │
│   ├── utils/
│   │   ├── ApiError.js                 # Custom error class with static factories
│   │   └── ApiResponse.js              # Standardized success/error response helpers
│   │
│   └── modules/
│       ├── auth/                       # Register · Login · Refresh · Logout · Me
│       │   ├── auth.controller.js
│       │   ├── auth.routes.js
│       │   ├── auth.service.js
│       │   └── auth.validator.js
│       ├── pdf/                        # Upload · List · Get · Delete · Text extraction
│       │   ├── pdf.controller.js
│       │   ├── pdf.routes.js
│       │   ├── pdf.service.js
│       │   └── pdf.upload.js
│       ├── flashcard/                  # Generate · Stream · CRUD
│       │   ├── flashcard.controller.js
│       │   ├── flashcard.routes.js
│       │   └── flashcard.service.js
│       ├── mcq/                        # Generate · Stream · Get
│       │   ├── quiz.controller.js
│       │   ├── quiz.routes.js
│       │   └── quiz.service.js
│       ├── summary/                    # Generate · Get · Export PDF
│       │   ├── summary.controller.js
│       │   ├── summary.routes.js
│       │   └── summary.service.js
│       ├── translation/                # Real-time Arabic translation
│       │   ├── translation.controller.js
│       │   └── translation.routes.js
│       ├── profile/                    # Get · Update · Password · Delete
│       │   ├── profile.controller.js
│       │   ├── profile.routes.js
│       │   └── profile.service.js
│       ├── HistoryFeature/             # Activity feed with full details
│       │   ├── history.controller.js
│       │   ├── history.routes.js
│       │   └── history.service.js
│       └── internal/                   # AI service registry (ngrok URLs)
│           └── registry.routes.js
│
├── prisma/
│   └── schema.prisma                   # Database schema
│
├── ai-service/                         # Python FastAPI microservice
│   ├── app.py                          # PDF extraction (PyMuPDF + Groq cleaning)
│   └── requirements.txt
│
├── .env                                # Environment variables
├── .env.example
├── package.json
├── package-lock.json
└── README.md
```

---

## 🤖 AI Services

### 1. Flashcard Generation Service
> Runs on Google Colab — URL registered dynamically at startup via `/api/internal/register-service`

**Model:** Fine-tuned `LLaMA-3-8B` with LoRA adapters trained on custom educational Q&A data (loaded via Unsloth for 2× faster inference)

**Key features:**
- Custom Alpaca-format prompt template
- Weighted difficulty distribution: `40% Easy · 40% Medium · 20% Hard`
- 5 question types per difficulty level
- JSON extraction with 2-strategy fallback (full match → field-by-field regex)
- In-session deduplication by question text
- Node.js chunks text into 2000-char blocks; Python sub-chunks to 600 chars for the model

**Endpoint:**
```
POST /generate-flashcard
Body: { "context": "...", "total_length": 0 }
```

### 2. MCQ Generation Service
> Runs on Google Colab — exposed via ngrok on **port 8001**

**Model:** Base `LLaMA-3-8B Instruct` (no fine-tuning, loaded via Unsloth)

**Key features:**
- Continuous 100% coverage chunking — zero gaps across the document
- `shuffle_options()` — removes answer bias (correct answer not always B)
- `_normalize_mcq()` — fixes malformed answers like `"A|D"` → `"A"`
- 2-strategy JSON extraction matching the flashcard service
- Separate `/generate-mcqs-chunk` endpoint for SSE streaming

**Endpoints:**
```
POST /generate-mcqs          → Regular generation (full text, internal chunking)
POST /generate-mcqs-chunk    → Streaming generation (pre-chunked by Node, limit=2)
```

### 3. PDF Extraction Service
> Runs locally on **port 8000** alongside the Node.js server (or same Colab tunnel)

**Stack:** PyMuPDF (text extraction) + Groq `llama-3.3-70b-versatile` (cleaning)

**What it does:**
1. Extracts raw text from PDF using PyMuPDF (fast, no external API needed for extraction)
2. Sends raw text to Groq to remove page numbers, footers, headers, and formatting artifacts
3. Returns clean, readable academic text ready for AI generation
4. Normalizes whitespace (collapses multiple newlines/spaces into single spaces)

**Endpoint:**
```
POST /extract
Body: multipart/form-data  →  file: <PDF>
Response: { "success": true, "text": "..." }
```

### 4. Summary Generation
> Direct Groq API call from Node.js backend

**Model:** `llama-3.3-70b-versatile`

**Prompt structure:** Enforces markdown sections — Main Topic, Key Concepts, Important Details, Conclusion. Parsed into structured JSON for the Flutter client.

### 5. Translation Service
> Direct Groq API call from Node.js backend

**Model:** `llama-3.3-70b-versatile`

**Features:**
- Translates any selected text to Arabic
- Max 5000 characters per request
- Returns ONLY the translated text (no explanations)

---

## 🚀 Backend API

### Tech Stack

| Layer | Technology |
|---|---|
| Runtime | Node.js 22.x |
| Framework | Express 5.x |
| ORM | Prisma |
| Database | PostgreSQL 16 |
| Authentication | JWT (access + refresh tokens) |
| AI — Summary & Translation | Groq `llama-3.3-70b-versatile` |
| Streaming | Server-Sent Events (SSE) |

---

## ⚙️ Getting Started

### Prerequisites

- Node.js ≥ 20
- PostgreSQL 16
- Python 3.11 (for local AI service)
- Google Colab account (for flashcard & MCQ services)
- ngrok (for exposing the backend)

### Installation

```bash
# 1. Clone the repository
git clone https://github.com/your-username/smart-study-buddy.git
cd smart-study-buddy

# 2. Install dependencies
npm install

# 3. Set up environment variables
cp .env.example .env
# Fill in all required values (see Environment Variables section)

# 4. Run database migrations
npx prisma migrate dev

# 5. Set up the Python AI service virtual environment (first time only)
cd ai-service
python -m venv venv
venv\Scripts\activate       # Windows
pip install -r requirements.txt
cd ..
```

### Running Everything

A single command starts the Node.js server, the Python AI service, and the ngrok tunnel together:

```bash
npm start
```

This uses `concurrently` to run all three processes in one terminal:

| Process | What it runs |
|---|---|
| `[0] ai` | Python FastAPI service on port 8000 |
| `[1] server` | Node.js / Express on port 3000 |
| `[2] tunnel` | ngrok tunnel exposing port 3000 |

### Running the Colab AI Services

1. Open the Flashcard notebook → Run all cells → Copy the ngrok URL → set as `FLASHCARD_SERVICE_URL` in `.env`
2. Open the MCQ notebook → Run all cells → Copy the ngrok URL → set as `MCQ_SERVICE_URL` in `.env`

---

## 🔐 Environment Variables

Create a `.env` file in the root directory:

```env
# ── Server ────────────────────────────────────────
NODE_ENV=development
PORT=3000

# ── Database ──────────────────────────────────────
DATABASE_URL=postgresql://user:password@localhost:5432/smart_study_buddy

# ── JWT ───────────────────────────────────────────
JWT_SECRET=your_jwt_secret_here
JWT_REFRESH_SECRET=your_refresh_secret_here
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# ── Bcrypt ────────────────────────────────────────
BCRYPT_ROUNDS=12

# ── AI Services ───────────────────────────────────
FLASHCARD_SERVICE_URL=https://xxxx.ngrok.io        # Colab flashcard ngrok URL
MCQ_SERVICE_URL=https://xxxx.ngrok.io              # Colab MCQ ngrok URL
GEMINI_API_KEY=your_gemini_api_key                 # For PDF extraction service
GROQ_API_KEY=your_groq_api_key                     # For summary & translation
```

---

## 📡 API Reference

### Authentication

| Method | Endpoint | Auth | Description |
|---|---|:---:|---|
| `POST` | `/api/auth/register` | ❌ | Register new user |
| `POST` | `/api/auth/login` | ❌ | Login and receive tokens |
| `POST` | `/api/auth/refresh` | ❌ | Refresh access token |
| `POST` | `/api/auth/logout` | ✅ | Logout and revoke token |
| `GET` | `/api/auth/me` | ✅ | Get current user profile |

### PDF Documents

| Method | Endpoint | Auth | Description |
|---|---|:---:|---|
| `POST` | `/api/pdfs` | ✅ | Upload PDF and extract text |
| `GET` | `/api/pdfs` | ✅ | List all user documents |
| `GET` | `/api/pdfs/:id` | ✅ | Get document with flashcards, quiz & summary |
| `GET` | `/api/pdfs/:id/text` | ✅ | Get extracted text only |
| `DELETE` | `/api/pdfs/:id` | ✅ | Delete document (cascades all related data) |

> **Duplicate detection:** If the same PDF content is uploaded again (even with a different filename), the server returns a `409 Conflict` with the original filename.

### Flashcards

| Method | Endpoint | Auth | Description |
|---|---|:---:|---|
| `POST` | `/api/pdfs/:id/flashcards` | ✅ | Generate flashcards (regular JSON) |
| `POST` | `/api/pdfs/:id/flashcards/stream` | ✅ | Generate flashcards **(SSE streaming)** |
| `GET` | `/api/pdfs/:id/flashcards` | ✅ | Retrieve all flashcards for a document |
| `PATCH` | `/api/flashcards/:id` | ✅ | Update a flashcard (front / back / difficulty) |
| `DELETE` | `/api/flashcards/:id` | ✅ | Delete a flashcard |

#### SSE Stream Events (Flashcards)

```
event: start       →  { totalChunks, fileName }
event: flashcards  →  { chunk, totalChunks, flashcards[] }
event: done        →  { document, count }
event: error       →  { message }
```

### Quiz (MCQ)

| Method | Endpoint | Auth | Description |
|---|---|:---:|---|
| `POST` | `/api/pdfs/:id/quiz` | ✅ | Generate quiz (regular JSON) |
| `POST` | `/api/pdfs/:id/quiz/stream` | ✅ | Generate quiz **(SSE streaming)** |
| `GET` | `/api/pdfs/:id/quiz` | ✅ | Retrieve latest quiz |

#### SSE Stream Events (Quiz)

```
event: start  →  { totalChunks, fileName }
event: mcqs   →  { chunk, totalChunks, mcqs[] }
event: done   →  { document, quizId, questionCount }
event: error  →  { message }
```

### Summary

| Method | Endpoint | Auth | Description |
|---|---|:---:|---|
| `POST` | `/api/pdfs/:id/summary` | ✅ | Generate summary |
| `GET` | `/api/pdfs/:id/summary` | ✅ | Retrieve latest summary |

### Translation

| Method | Endpoint | Auth | Description |
|---|---|:---:|---|
| `POST` | `/api/translate` | ✅ | Translate selected text to Arabic |

**Request:**
```json
{ "text": "Selected text from summary (max 5000 chars)", "targetLang": "ar" }
```

**Response:**
```json
{
  "original": "...",
  "translated": "...",
  "targetLang": "ar"
}
```

### Profile

| Method | Endpoint | Auth | Description |
|---|---|:---:|---|
| `GET` | `/api/profile` | ✅ | Get user profile |
| `PATCH` | `/api/profile` | ✅ | Update display name |
| `PATCH` | `/api/profile/password` | ✅ | Change password |
| `DELETE` | `/api/profile` | ✅ | Delete account (cascades everything) |

### Internal

| Method | Endpoint | Auth | Description |
|---|---|:---:|---|
| `POST` | `/api/internal/register-service` | 🔑 | Register Colab AI service URL |

> Protected by `x-internal-secret` header. Called automatically by Colab notebooks at startup.

---

## 📁 Project Structure

```
smart-study-buddy-backend/
│
├── src/
│   ├── app.js                          # Express app setup & route mounting
│   ├── server.js                       # Server entry point
│   │
│   ├── config/
│   │   └── env.js                      # Environment variable validation
│   │
│   ├── prisma/
│   │   └── client.js                   # Prisma client singleton
│   │
│   ├── middleware/
│   │   ├── authenticate.js             # JWT verification middleware
│   │   ├── errorHandler.js             # Global error handler
│   │   └── notFound.js                 # 404 handler
│   │
│   ├── utils/
│   │   ├── ApiError.js                 # Custom error class
│   │   └── ApiResponse.js              # Standardized response helpers
│   │
│   └── modules/
│       ├── auth/                       # Register · Login · Refresh · Logout
│       ├── pdf/                        # Upload · List · Get · Delete
│       ├── flashcard/                  # Generate · Stream · CRUD
│       ├── mcq/                        # Generate · Stream · Get
│       ├── summary/                    # Generate · Get
│       ├── translation/                # Real-time Arabic translation
│       ├── profile/                    # Get · Update · Password · Delete
│       └── HistoryFeature/             # Document history with full details
│
├── prisma/
│   └── schema.prisma                   # Database schema
│
├── ai-service/
│   ├── app.py                          # PDF extraction (PyMuPDF + Groq)
│   └── requirements.txt
│
├── .env.example
├── package.json
└── README.md
```

---

## 🗄 Database Schema (Overview)

```
User
 ├── PdfDocument []
 │    ├── Flashcard []
 │    ├── Quiz []
 │    │    └── Question []
 │    └── Summary []
 └── refreshToken
```

All child records are cascade-deleted when a `PdfDocument` is removed.

---

## 🧪 Testing with Postman

1. **Register** → `POST /api/auth/register`
2. **Login** → `POST /api/auth/login` → copy the `accessToken`
3. Add header: `Authorization: Bearer <accessToken>` to all protected requests
4. **Upload PDF** → `POST /api/pdfs` (form-data, key: `file`)
5. **Stream Flashcards** → `POST /api/pdfs/:id/flashcards/stream`

> For SSE endpoints, use `curl` with the `-N` flag:
> ```bash
> curl -N -X POST http://localhost:3000/api/pdfs/1/flashcards/stream \
>   -H "Authorization: Bearer <token>"
> ```

---

## 👥 Team

Built as a graduation project — Faculty of Computers and Information Sciences.

---

<div align="center">
  <sub>Built with ❤️ using Node.js · FastAPI · LLaMA-3 · Unsloth · Groq</sub>
</div>
